datasets==3.6.0
datasketch==1.6.4
Flask==3.0.3
Flask_Cors==4.0.0
jieba==0.42.1
jsonlines==4.0.0
marshmallow==3.22.0
ngrok==1.4.0
nltk==3.8
numpy==1.26.4
openai==1.59.6
psutil==5.9.8
protobuf==6.33.5
pydantic==2.11.5
pyyaml==6.0.2
rich==13.7.1
scikit_learn==1.5.1
sentence_transformers==2.3.1
sentencepiece==0.2.0
simhash==2.1.2
tiktoken==0.10.0
transformers==4.57.6
jinja2==3.1.2
jsonlines==4.0.0
trl==0.13.0
ujson==5.1.0
wandb==0.22.3
streamlit==1.50.0
einops==0.8.1
swanlab==0.9.8
modelscope==1.37.0
bitsandbytes==0.47.0  # QLoRA 的 NF4 量化与 paged 8-bit 优化器
# torch==2.6.0
# torchvision==0.21.0
# peft==0.7.1

# ---------------------------------------------------------------- 新增部分
# 下面是 webui/、scripts/、test/storage/ 引入的依赖。
# 主线训练（trainer/train_*.py）用不到，但跑这些模块时必须装上。
# ----------------------------------------------------------------

# webui/server.py 与 scripts/serve_openai_api.py：WebUI / OpenAI 兼容服务端（FastAPI + SSE）
fastapi==0.115.6
uvicorn==0.32.1

# test/storage/：报告绘图（report_*.py / plot_training_log.py）与读 parquet 评测
matplotlib==3.10.0
pandas==2.2.3

# test/storage/：下载外部数据集（download_extra_datasets.py）
huggingface_hub==0.27.0

# test/storage/：lm-evaluation-harness 适配层（minimind_lm.py）。
# 只有想切到 lm_eval CLI 时才需要；eval_benchmarks.py 走本地 parquet，不依赖它。
# lm_eval==0.4.5
